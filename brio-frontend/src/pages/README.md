# pages/

Componentes de página, ligados diretamente a uma rota (ex:
DashboardPage.tsx -> rota "/dashboard"). Uma page é fina: ela só
monta a página combinando componentes de features/ -- não deve ter
lógica de negócio nem chamadas de API diretas.
