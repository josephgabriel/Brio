# lib/

Utilidades compartilhadas por todo o app:
- api-client.ts   -> instância configurada de chamada HTTP (baseURL da API, headers)
- query-client.ts -> configuração do TanStack Query (cache, retries)
- utils.ts        -> funções puras reutilizáveis (formatação de data, etc.)
